version: '3.8'

services:
  backend_rds:
    build:
      context: ./backend_rds
      dockerfile: Dockerfile
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DEBUG=${DEBUG}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST}  # Use your actual DB host for production (e.g., private IP or RDS endpoint)
      - DB_PORT=5432
      - CORS_ALLOWED_ORIGINS=http://d2l70ql7e72i7p.cloudfront.net
      - DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
    ports:
      - "8000:8000"
    networks:
      - backend_network

  backend_redis:
    build:
      context: ./backend_redis
      dockerfile: Dockerfile
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DEBUG=${DEBUG}
      - REDIS_HOST=${REDIS_HOST}  # Use your actual Redis host for production (e.g., private IP or Redis service endpoint)
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_DB=${REDIS_DB}
      - CORS_ALLOWED_ORIGINS=http://d2l70ql7e72i7p.cloudfront.net
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    ports:
      - "8001:8001"
    networks:
      - backend_network

  # If you're using PostgreSQL as a local container for development:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - backend_network

  # If you're using Redis as a local container for development:
  redis:
    image: redis:alpine
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    command: ["redis-server", "--requirepass", "${REDIS_PASSWORD}"]
    ports:
      - "6379:6379"
    networks:
      - backend_network

# Define custom network for communication between services
networks:
  backend_network:
    driver: bridge

# Persist PostgreSQL data across container restarts
volumes:
  pgdata:
